<%= form_tag("/relationships/#{@user.id}", id: "unfollow_button", method: :delete) do %>
	<%= hidden_field_tag :followed_id, @user.id %>
	<%= submit_tag 'Unfollow', class: "btn btn-large btn-danger",  data: { keybinding: '[command+enter]' } %>
<% end %>